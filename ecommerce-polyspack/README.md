# Polyspack Enterprises Monorepo

## Structure

- `frontend/` — Next.js app (customer + admin dashboard)
- `backend/` — Node.js/Express API
- `shared/` — Shared code (types, utils, etc.)

## Setup

1. Copy `.env.example` to `.env` and fill secrets.
2. See each subfolder for specific instructions.
